### Shell environment set up for builds. ###

You can now run 'bitbake <target>'
Targets for building Tegra demo images with test applications:
    final-project-image - X11/sato UI plus docker, openCV, multimedia API samples
